apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-dns
  namespace: argocd
spec:
  destination:
    name: ""
    namespace: external-dns
    server: "https://kubernetes.default.svc"
  source:
    repoURL: https://github.com/tomberget/aks-sandbox.git
    path: ./apps/external-dns
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=false
